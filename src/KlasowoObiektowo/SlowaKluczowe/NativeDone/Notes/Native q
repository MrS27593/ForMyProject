## QUIZ – `native` w Java (10 pytań)

---

### **1. Co oznacza słowo kluczowe `native` w Javie?**

A. Metoda jest implementowana w języku Java
B. Metoda jest implementowana w innym języku, np. C/C++
C. Klasa jest natywna
D. Zmienna jest statyczna

---

### **2. Co jest prawdziwe dla metod `native`?**

A. Mogą mieć ciało w Javie
B. Nie mogą mieć ciała w Javie
C. Mogą być tylko prywatne
D. Mogą być tylko statyczne

---

### **3. Które elementy mogą być oznaczone słowem `native`?**

A. Pola
B. Klasy
C. Metody
D. Konstruktory

---

### **4. Dlaczego używa się `native`?**

A. Aby korzystać z funkcji systemowych lub bibliotek napisanych w innych językach
B. Aby tworzyć zmienne tymczasowe
C. Aby deklarować klasy finalne
D. Aby tworzyć tablice

---

### **5. Która deklaracja jest poprawna?**

A. `public native void doSomething() { }`
B. `public native void doSomething();`
C. `native int x;`
D. `native class Example {}`

---

### **6. Jaką bibliotekę należy załadować, aby użyć metody `native`?**

A. Bibliotekę Java standardową
B. Bibliotekę natywną (np. C/C++) poprzez `System.loadLibrary()`
C. Nie trzeba ładować żadnej biblioteki
D. Bibliotekę z JDK tylko

---

### **7. (OTWARTE)**

Podaj przykład zastosowania metody `native` w Javie, np. do pobrania informacji o systemie operacyjnym.

---

### **8. (OTWARTE)**

Dlaczego metody `native` nie mają ciała w Javie?

---

### **9. Czy metody `native` mogą być `static`?**

A. Tak
B. Nie
C. Tylko w klasach abstrakcyjnych
D. Tylko w interfejsach

---

### **10. Które zdanie o `native` jest prawdziwe?**

A. `native` można stosować do pól i klas
B. `native` umożliwia integrację z kodem natywnym
C. `native` oznacza, że metoda jest abstrakcyjna
D. `native` powoduje automatyczne wywołanie konstruktora

---

Napisz **„odpowiedzi”**, a podam komplet poprawnych odpowiedzi z wyjaśnieniami ✅
